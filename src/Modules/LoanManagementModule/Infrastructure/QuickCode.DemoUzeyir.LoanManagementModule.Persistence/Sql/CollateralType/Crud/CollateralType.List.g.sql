SELECT
    [ID],
    [CODE],
    [NAME],
    [IS_ACTIVE]
FROM [dbo].[COLLATERAL_TYPES]
WHERE [IsDeleted] = 0
ORDER BY
    [ID]
OFFSET @PRM_QC_LIST_OFFSET ROWS
FETCH NEXT @PRM_QC_LIST_PAGE_SIZE ROWS ONLY;