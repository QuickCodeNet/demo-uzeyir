SELECT
    [ID],
    [CODE],
    [NAME],
    [SYMBOL],
    [IS_ACTIVE]
FROM [dbo].[CURRENCIES]
WHERE [IsDeleted] = 0
ORDER BY
    [ID]
OFFSET @PRM_QC_LIST_OFFSET ROWS
FETCH NEXT @PRM_QC_LIST_PAGE_SIZE ROWS ONLY;